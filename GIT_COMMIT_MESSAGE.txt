fix: AI background removal production build with WASM support

Problem:
- Background removal feature works in localhost but fails in production
- Error: "Gagal memproses AI. Pastikan file bukan format HEIC atau terlalu besar"
- Root cause: ONNX Runtime Web WASM files not copied to dist/ folder

Solution:
- Add explicit onnxruntime-web dependency to package.json
- Create Vite plugin to auto-copy WASM files from node_modules to dist/
- Configure publicPath for @imgly/background-removal in production
- Add assetsInlineLimit: 0 to prevent inlining WASM files
- Improve error messages with detailed logging

Changes:
- package.json: Add onnxruntime-web@^1.21.0 dependency
- vite.config.ts: Add copy-onnx-wasm plugin + build config
- frontend/src/pages/admin/DevIDCardTest.tsx: Configure publicPath
- scripts/verify-wasm-build.mjs: Add verification script
- docs/runbooks/AI-BACKGROUND-REMOVAL-FIX.md: Technical documentation
- docs/deployment/AI-BACKGROUND-REMOVAL-DEPLOYMENT.md: Deployment guide
- QUICK_FIX_HAPUS_BACKGROUND.md: Quick reference
- AI_BACKGROUND_REMOVAL_FIX_SUMMARY.md: Complete summary

Testing:
- npm run build - Should show "✅ Copied *.wasm to dist/"
- npm run verify:wasm - Verify WASM files present
- npm run preview - Test locally before deploy

Performance:
- First load: 10-30s (downloads AI model from CDN)
- Subsequent: 2-5s (uses cached model)
- Build size: +9.4 MB (WASM files)

Deployment:
1. Build and verify locally
2. Test with npm run preview
3. Deploy to production
4. Verify WASM files accessible at root domain
5. Test background removal feature

Fixes background removal not working in production environment.
